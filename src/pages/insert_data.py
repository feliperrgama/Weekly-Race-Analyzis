from os import system

# Limpar a tela do menu principal para somente aparecer a tela de inserção de dados
system('clear') 

distance = float(input('Distância em quilometros: '))
avarege_pace_per_km = str()
# moving_time varchar(10) not null,
# elevation_gain tinyint not null,
# max_elevation smallint not null,
# date datetime not null,
# now_date timestamp default current_timestamp on update current_timestamp,
# day_of_the_week varchar(50)



if __name__ == '__app__':
